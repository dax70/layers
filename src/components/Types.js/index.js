
/* @flow */

export type Point = {
    x: Number,
    y: Number
}

export type Bounds = {
  width: Number,
  height: Number
}

export type Rect = {
  top: number,
  left: number,
  width: number,
  height: number,
  bottom: number,
  right: number,
};
